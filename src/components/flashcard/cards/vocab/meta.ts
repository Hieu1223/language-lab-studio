export const cardType = "vocab" as const;
/** English fallback used when i18n has not resolved `labelKey`. */
export const label = "Vocab";
/** Namespaced so `t(labelKey)` resolves against the `flashcard` catalogue. */
export const labelKey = "flashcard:card.vocab";
